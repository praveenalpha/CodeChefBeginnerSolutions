#include <bits/stdc++.h>
using namespace std;

int main() {
	
	int t,a,i;
	cin>>t;
	for(i=0;i<t;i++)
	{
	    cin>>a;
	    if(a<10)
	    {cout<<"What an obedient servant you are!"<<endl;}
	    else
	    {cout<<"-1"<<endl;}
	}
	return 0;
}

