#include <bits/stdc++.h>
using namespace std;
int main()
{int t,n,c,d=0;
scanf("%d",&t);
for(int i=0;i<t;i++)
{scanf("%d",&n);
d=0;
while(n!=0)
{c=n%10;
d=(d*10)+c;
n=n/10;
}
printf("%d\n",d);
}
return 0;
}
