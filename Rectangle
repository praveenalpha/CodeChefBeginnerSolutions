#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--)
	{
	    int a[4];
	    cin>>a[0]>>a[1]>>a[2]>>a[3];
	    
	    sort(a,a+4);
	    
	    if(a[0]!=a[1] || a[2]!=a[3])
	    cout<<"NO"<<endl;
	    else
	    cout<<"YES"<<endl;
	}
	return 0;
}
